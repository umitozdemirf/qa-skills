description = "Generate API test suites using native unit test frameworks (pytest, vitest/jest, JUnit, go test) directly — no intermediate tools."

prompt = """Use the skill definition at `qa-skills-plugin/skills/api-test-generator/SKILL.md`.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/api-test-generator/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: {{args}}
If `{{args}}` is empty, infer the target from the current conversation and repository context.
"""
