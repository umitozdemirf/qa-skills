description = "Analyze test coverage gaps, identify untested code paths, and recommend missing test scenarios."

prompt = """Use the skill definition at `qa-skills-plugin/skills/test-coverage-analyzer/SKILL.md`.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/test-coverage-analyzer/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: {{args}}
If `{{args}}` is empty, infer the target from the current conversation and repository context.
"""
