description = "Analyze code changes for risk impact, identify fragile areas, and recommend testing priorities."

prompt = """Use the skill definition at `qa-skills-plugin/skills/risk-analyzer/SKILL.md`.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/risk-analyzer/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: {{args}}
If `{{args}}` is empty, infer the target from the current conversation and repository context.
"""
