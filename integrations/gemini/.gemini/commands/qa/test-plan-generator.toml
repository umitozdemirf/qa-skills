description = "Generate structured test plans with risk-based prioritization for features, PRs, or releases."

prompt = """Use the skill definition at `qa-skills-plugin/skills/test-plan-generator/SKILL.md`.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/test-plan-generator/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: {{args}}
If `{{args}}` is empty, infer the target from the current conversation and repository context.
"""
