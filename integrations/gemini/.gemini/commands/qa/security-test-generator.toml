description = "Generate OWASP-based security test cases for web applications and APIs."

prompt = """Use the skill definition at `qa-skills-plugin/skills/security-test-generator/SKILL.md`.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/security-test-generator/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: {{args}}
If `{{args}}` is empty, infer the target from the current conversation and repository context.
"""
