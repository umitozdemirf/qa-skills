description = "Generate boundary value, equivalence partitioning, and edge case test data for input validation testing."

prompt = """Use the skill definition at `qa-skills-plugin/skills/input-validation-tester/SKILL.md`.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/input-validation-tester/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: {{args}}
If `{{args}}` is empty, infer the target from the current conversation and repository context.
"""
