---
description: Run the `test-coverage-analyzer` QA workflow against the current task
---

Read `qa-skills-plugin/skills/test-coverage-analyzer/SKILL.md` from the current repository.

Follow its deterministic execution flow exactly.
Load only the references you need from `qa-skills-plugin/skills/test-coverage-analyzer/references/`.
Treat `examples/`, `scripts/`, `test/`, and `assets/` as optional helpers when they exist.

Apply the skill to this request: $ARGUMENTS
If `$ARGUMENTS` is empty, infer the target from the current conversation and repository context.
